deploy:
    needs: test
    runs-on: self-hosted

    steps:
      - name: Deploy Application
        run: |
          cd ~/my-app
          git pull origin main
          npm install
          pm2 restart app
